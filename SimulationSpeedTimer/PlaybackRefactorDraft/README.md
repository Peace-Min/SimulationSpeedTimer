# Playback Refactor Draft

This project is a non-compiling draft container for the rewritten `UserCustomChart_2DGraph` playback logic.

- It is intentionally excluded from compilation.
- The goal is to keep the refactored code in a separate place for comparison and manual porting.
- External chart dependencies and original project references are not included here.
